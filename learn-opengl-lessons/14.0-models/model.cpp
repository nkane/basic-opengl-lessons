#include <vector>
#include <string>

/*
#include "texture.cpp"
#include "mesh.cpp"

typedef struct _model
{
    std::vector<Texture> textures_loaded;
    std::vector<Mesh> meshes;
    std::string directory;
} Model;
*/
